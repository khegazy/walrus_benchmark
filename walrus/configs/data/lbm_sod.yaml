defaults:
- field_index_map_override: lbm_field_index
well_base_path: null
wandb_data_name: lbm_sod
module_parameters:
  _target_: walrus.data.MixedWellDataModule
  batch_size: 8
  n_steps_input: 10
  n_steps_output: 1
  min_dt_stride: 1
  max_dt_stride: 1
  max_samples: 2000
  max_rollout_steps: 200
  well_dataset_info:
    lbm_sod:
      include_filters: []
      exclude_filters: []
      path: datasets/kinet/sod/
